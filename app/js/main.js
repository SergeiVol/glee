$(function (){

  $('.top-slider__items').slick({
    dots: true,
    arrows: false,
    fade: true,
    // autoplay: true,
    // autoplaySpeed: 2000
  });

  let mixer = mixitup('.catalog__items');

});